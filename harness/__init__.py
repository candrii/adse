"""ai-harness — the headless-agent sandbox runtime.

This package exposes one module:
  - sandbox: thin CLI over `docker compose` for per-project sandboxes.

The orchestration layer (Temporal, Runtime Manager, MCP server) was
explored during the design phase but pulled back per the brief — see
docs/out-of-scope/.
"""
